const url = 'mongodb://localhost:27017/';
const Name = 'devconnector';
const secretKey='secret'
//mongoose.Promise = global.Promise;
//const connect=mongoose.connect(url + dbName);


module.exports={
    url,
    Name,
    secretKey
}